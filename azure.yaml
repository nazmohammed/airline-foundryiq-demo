# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: airline-foundryiq-demo
metadata:
  template: airline-foundryiq-demo@1.0.0

infra:
  provider: bicep
  path: infra
  module: main

services:
  backend:
    project: app/backend
    language: python
    host: containerapp
